# Action Environments
# This folder contain mini project that deploy workflow yaml files with cloudFormation yaml files, that enable to automate ressources creation in cloud Infra as AWS.
# it present inputs notions in file .github/workflows/artifacts_workflow.yml when we have files index.html and error.html files as inputs
# secret variables as used in majority of workflows files like in .github/workflows/secrets_workflow.yml and that necessite to add these secret to guthub as repository secrets
# deploying in diffrents environements like staging and production as we did in workflow: .github/workflows/environment-workflow.yml

